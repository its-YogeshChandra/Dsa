let ar = [5, 2, 8, 1, 3];

const bubblesort = () => {
  //fix the array
  let i = 0;
  let j = i + 1;


  //fix the array
  if (arr[i] < arr[j]) {
    let temp = arr[i]
    arr[i] = arr[j]
    arr[j] = temp
  }
  i++
  j++

}

cosnole.log(bubblesort())
